# Local development defaults. Keep personal keys in .envrc.local.
if [ -f .envrc.local ]; then
  set -a
  source .envrc.local
  set +a
fi

export PORT="${PORT:-3000}"
export MCP_URL="${MCP_URL:-http://localhost:${PORT}}"
